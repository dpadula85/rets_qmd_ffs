Notes
=====

This folder contains rPSB force fields fitted with the Joyce program.
The `ret.top` file is an example topology, in which any of the include `.itp`
files can be incorporated to model a different rPSB.
